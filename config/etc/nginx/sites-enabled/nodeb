upstream backend_servers{
        server apib:3000;
    }

upstream es_server{
        server es1b:9200;
    }

upstream ganglia_server{
        server monitorb:3000;
    }

upstream nagios_server{
        server monitorb:4000;
    }